---
- name: k3s cluster foundation
  hosts: k3s_cluster
  gather_facts: true
  roles:
    - node-prep
    # - k3s        # enabled in Task 2
    # - metallb    # enabled in Task 3
    # - longhorn   # enabled in Task 4
