# --- Basics ---
TZ=America/Los_Angeles
ACME_EMAIL=you@2bit.name

# --- Network (macvlan on VLAN 60) ---
SYNOLOGY_INTERFACE=eth0.60
VLAN60_SUBNET=10.20.60.0/24
VLAN60_GATEWAY=10.20.60.1
CADDY_IP=10.20.60.10

# --- Porkbun DNS API (for ACME DNS-01) ---
# Create a scoped API key in Porkbun for DNS updates on 2bit.name
PORKBUN_API_KEY=change_me
PORKBUN_API_SECRET_KEY=change_me

# --- Gitea upstream ---
GITEA_UPSTREAM_HTTP_ADDR=10.20.60.11
# If you prefer per-host blocks instead of wildcard, set this and uncomment the site in Caddyfile
GITEA_FQDN=git.lab.2bit.name

# Data locations (persist certs/config)
CADDY_DATA=./data
CADDY_CONFIG=./config
